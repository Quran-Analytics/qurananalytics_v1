% preamble
% natbib must be prior to other packages
\usepackage{natbib}
\usepackage{booktabs}
\usepackage{longtable}
%\usepackage[document]{ragged2e} % for fully justified paragraphs, must be before polyglossia
% add arabic script package
\usepackage{fontspec}
\usepackage{polyglossia} % to allow multiple type fonts
\setmainlanguage{english}
\setotherlanguage{arabic}
\newfontfamily\arabicfont[Script = Arabic]{Amiri} % can choose other Arabic scripts
